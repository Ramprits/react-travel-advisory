const Home = () => {
  return (
    <div
      style={{
        display: "flex",
        flexDirection: "column",
        minHeight: "100vh",
      }}>
      <div>Welcome to superplate</div>
    </div>
  );
};

export default Home;
